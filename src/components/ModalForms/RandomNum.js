

      export default function Random() {
        let maxNumber = 100000;
        let randomNumber = Math.floor((Math.random() * maxNumber) + 100000);
        return(
        
          // Random number generated

        <div>Confirmation #{randomNumber}</div>);
        
        
      }